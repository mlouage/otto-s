import React from "react"

const Main = ({children}) => (
    <div className="bg-gray-200">
        <div className="p-8">
            {children}
        </div>
        
    </div>
)

export default Main;